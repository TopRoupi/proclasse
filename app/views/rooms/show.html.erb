<div class="flex justify-between items-center">
  <h1 class="text-4xl font-bold">Alunos</h1>
</div>

<% @requests.each do |room_request| %>
  <%= render RoomRequestCardComponent.new(room_request) %>
<% end %>


<% @students.each do |student| %>
  <%= render StudentCardComponent.new(student, @room) %>
<% end %>
